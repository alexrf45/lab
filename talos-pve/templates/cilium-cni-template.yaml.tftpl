cluster:
  inlineManifests:
    - name: cilium-values
      contents: |
        ---
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: cilium-values
        namespace: kube-system
      data:
        values.yaml: |-
          ${indent(cilium_values)}
    - name: cilium-bootstrap
      contents: |
        ${indent(6, cilium_install)}
